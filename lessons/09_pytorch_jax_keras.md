# PyTorch
* Imported as `torch`
* Linear classifier example

# JAX
* Functions are stateless
  * Complications for pseudorandom number generation
* Gradient computation uses higher-order functions
* Linear classifier example


* Discussion: relative advantages of Tensorflow, PyTorch, and JAX

# Keras
* Layers
  * Automatic shape inference
* Models
  * First glimpse of Transformer!
* Compiling with 'compile'
* Training with `fit`
  * Can provide validation data
  * `fit` returns a `History` object
* Predicting with `predict`